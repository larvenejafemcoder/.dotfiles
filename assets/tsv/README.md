# TSV

Tab-separated package and command lists (mirrors CSV data in TSV format).

Includes: brew packages, brew casks, environments, install packages, main commands, package managers, setup packages.
