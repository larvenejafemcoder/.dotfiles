# CI

Continuous integration and Docker files for testing the dotfiles across distros.

- `Brewfile` — macOS Homebrew packages for CI
- `docker/` — Dockerfiles for Arch, Fedora, Ubuntu CI testing
- `homebrew/` — Brewfile for Homebrew-based testing
- `workflows/` — GitHub Actions workflows (cloc, pages deployment)
