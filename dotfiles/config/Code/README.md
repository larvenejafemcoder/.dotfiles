# Code (VS Code)

Visual Studio Code / VS Codium user settings.

- `settings.json` — Editor preferences (font, theme, extensions, keybindings)
- `keybindings.json` — Custom keyboard shortcuts
- `apply.sh` — Applies settings to VS Code/VS Codium config directory
- `myextensions/` — Extension list export/import scripts
