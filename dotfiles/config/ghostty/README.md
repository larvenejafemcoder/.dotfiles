# Ghostty

Config for Ghostty — a GPU-accelerated terminal emulator.

- `config` — Main config (JetBrains Mono, blur, block cursor, keybindings)
- `opacity.conf` — Window opacity settings (imported by config)
- `themes/dots` — Custom color theme
- `scripts/show-ghostty-config.sh` — Utility to display current config
