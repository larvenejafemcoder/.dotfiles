# Fonts

Nerd Font-patched fonts for terminal and IDE use.

- `font.sh` — Downloads Meslo Nerd Font from the latest Nerd Fonts release
- `Meslo/` — Meslo LG Nerd Font .ttf files (72 variants) + local install script

Installed to `~/.local/share/fonts`.
