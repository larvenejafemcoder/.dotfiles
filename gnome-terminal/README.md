# GNOME Terminal

dconf dump/restore backup for GNOME Terminal profiles.

- `gnome-terminal.dconf` — Export from `dconf dump /org/gnome/terminal/`

Restore: `dconf load /org/gnome/terminal/ < gnome-terminal/gnome-terminal.dconf`
