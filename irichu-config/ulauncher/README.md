# Ulauncher

Config for Ulauncher — an application launcher with fuzzy search, extensions, and custom themes.

- `settings.json` — General preferences (hotkey, theme, autostart)
- `extensions.json` — Installed extensions config
