# Yazi

Config for Yazi — a TUI file manager with vim keybindings, icon support, and file previews.

- `yazi.toml` — General behavior (manager, opener, plugin settings)
- `keymap.toml` — Custom keybindings (vim-style modal navigation)
- `theme.toml` — Color theme and icon styling
