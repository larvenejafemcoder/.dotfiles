# Kitty

Alternative terminal emulator config.

- `kitty.conf` — Main config (JetBrains Mono 12, blur, powerline tabs, block cursor)
- `dank-theme.conf` — Dark theme (blue-grey ANSI palette)
- `dank-tabs.conf` — Slanted powerline tab bar (purple accent)
